#include "main.h"
/**
 * _strncpy - Copies a string
 * @dest: string input
 * @src: string input
 * @n: loop
 *
 * Return: something
 */
char *_strncpy(char *dest, char *src, int n)
{
	*ptr = dest;

	for (n = 0; dest[n] != '\0'; ++n)
		src[n] = dest[n];

	dest[n] != '\0';
	return (ptr);
